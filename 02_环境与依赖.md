# 02 · 环境与依赖

## 需要安装的东西

### 1. REAPER（必须）
- 本体装好即可。分轨渲染需要推子/导出操作，全部由脚本驱动。
- **开启 REAPER 的 Distant API**：REAPER → Options → Preferences → Control/OSC/web → Add → 选 **Distant API**。reapy 走这个通道连接。（2026-09-14 修订：端口不固定，连接统一用 `skill快照/reaper-foley-workflow/scripts/connect_reaper.py --launch` 自适应直连——自动找进程→netstat 枚举端口→实连；REAPER 未运行时 AI 可自行启动，制作人已解禁）
- 端口不要写死（2308/2309 均实测出现过），以 connect_reaper.py 自动发现为准。

### 2. Python（3.10+ 均可）
```
pip install reapy_boost
```
- reapy_boost 是 reapy 的增强 fork，支持中断恢复。**必须用 reapy_boost，不要用原版 reapy**（原版在长任务里会随机断连）。

### 3. ffmpeg（导出终验用）
- 只用来跑 `ffprobe`（验格式）和 `ffmpeg -i x.wav -af volumedetect -f null -`（验电平）。装好并在 PATH 里即可。

### 4. 检索引擎依赖（重建索引时才需要）
```
pip install sqlite3  # 内置，无需装
# FTS5 已随 Python 自带 sqlite 编译；若报 no such module: fts5 需换 Python 版本
```

## 连接自检（每次开工第一件事）

```python
import time
from ipaddress import IPv4Address
import reapy_boost
from reapy_boost.tools.network.machines import Host

reapy_boost.connect(Host(IPv4Address("127.0.0.1")))
RPR = reapy_boost.reascript_api
p = reapy_boost.Project()
time.sleep(1)  # 连接后必须等一下，立刻调用会偶发失败
print(RPR.GetProjectName("", 512))
```

能打印出工程名 = 通道正常。

## 参考实测版本（2026-09-16）

| 组件 | 实测版本 |
|---|---|
| REAPER | 7.52（命令行可带工程路径启动） |
| reapy_boost | 0.10.201（依赖 typing_extensions 一并装） |
| Python | 3.10~3.12（**3.13 有坑**：reapy RPC 挂起/爆栈，需 08 号坑表的补丁，能避开就避开） |
| ffmpeg/ffprobe | 2026-07 构建（新版用 `-fps_mode vfr` 替代已弃用的 `-vsync vfr`） |

## 平台说明

- 原机是 **Windows**。所有路径写法、Task Scheduler 等细节都是 Windows 口径。其他平台理论上可行，但 REAPER API 行为与坑位只在 Windows 实测过。
- reapy 偶发段错误（exit 1 零输出）是已知现象，重跑即可——脚本必须**可安全重跑**（幂等），见 05 号文档的防重设计。
