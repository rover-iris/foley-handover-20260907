# 00 · 从这里开始（给 AI 助手的第一封信）

> **你是什么**：你是一台 AI 助手（任何品牌/载体都可以），你的任务是接手「AI 驱动 DAW 影视音频工作流」，为动画剧集项目做音效贴轨与分轨导出。
> **关于通用性**：本包是一套**与具体剧集无关的通用方法论**。文中出现的一切具体工程文件、目录、集数，都是历史项目的实例占位——实际路径与项目参数**开工时问人类要**，方法本身照搬即可。
> **交接人**：原操作者的 AI 助手（AI 助手）。本文件夹自包含，不需要读取原机器的任何其他文件。

---

## 新环境部署（三步）

1. **安装三个 skill**：把 `skill快照/` 下三个子目录整体拷到 `~/.agents/skills/`（或你的 agent 平台的 skill 目录）。快照与活体 skill 完整一致（含全部脚本），拷完即可用。
2. **装依赖**：REAPER（开 Distant API）+ `pip install reapy_boost` + ffmpeg，详见 02 号。
3. **音源库**：`scripts/pipeline/asset_library.db` 开箱即用；音源盘路径不同则按 04 号「路径问题」批量替换前缀或重建索引。

## 阅读顺序（按此依次读完再动手）

| 序号 | 文件 | 内容 |
|------|------|------|
| 01 | `01_项目概览与铁律.md` | 工作模式、工程结构约定、绝对不可违反的铁律 |
| 02 | `02_环境与依赖.md` | REAPER / Python / reapy_boost / ffmpeg 安装与连接自检 |
| 03 | `03_每集标准流程SOP.md` | 一集从报告到贴轨完成的完整流程 |
| 04 | `04_检索引擎与选材.md` | 音源库 DB 的用法、选材规则（含 20260916 全部口径） |
| 05 | `05_贴轨脚本与自检.md` | place 脚本怎么写、cue 格式、自检方法 |
| 06 | `06_分轨导出.md` | 唯一可行的导出方案（踩过无数坑后定稿） |
| 08 | `08_坑与FAQ速查.md` | 所有踩过的坑，出问题先查这里 |
| 09 | `09_贴音效步骤与效率纪律.md` | 贴音效五步流程 + 「模板定稿、每集只填数据表」+ 多 agent 派活纪律。**动手贴轨前必读** |
| 附录 | `12_变更记录.md` | 历史变更日志（只记历史不含规则；查「哪天改了什么」用） |

**上游输入规范**：逐秒动作分析报告的生成规范与出厂门禁在 `skill快照/video-action-analysis/SKILL.md`——接上游报告先跑它的 `check_report.py` 门禁，FAIL 打回。

## 本文件夹结构

```
音效工作流交接包_20260907/
├── 00_从这里开始_README.md   ← 你在这里
├── 01~09, 12_*.md            ← 成套方法论文档（08 是坑速查；12 是变更日志）
├── skill快照/                ← 三个 skill 的完整快照（SKILL.md + references + scripts，可直接安装）
│   ├── reaper-foley-workflow/      ← 贴轨方法论权威（01~09 冲突时以它为准）
│   ├── video-action-analysis/      ← 上游逐秒报告生成规范 + 出厂门禁
│   └── reaper-remote-ops/          ← Python 驱动 REAPER 操作手册（动手前先读）
├── scripts/
│   ├── pipeline/             ← 检索引擎全套源码 + 音源库 DB（61,347 条索引）
│   └── 示例与工具/           ← 真实用过的贴轨/导出/审计/Region 脚本（当模板抄）
└── assets/                   ← 项目侧数据样例（region_bases CSV 由 extract_regions.py 生成）
```

## 给 AI 助手的三句话总纲

1. **上游输入**是「逐秒动作分析报告」（带帧证据），**下游产出**是贴好 cue 的 REAPER 工程 + 48k/24bit 分轨 wav。
2. 一切自动化通过 `reapy_boost` 连接本机 REAPER 实现——但 REAPER 的 API 有很多反直觉的坑，**没写进文档的 API 用法不要凭想象用**，先查 08 号文档与 skill 快照。
3. 每一步做完都要**自检对账**（数量对不上 = 有事发生），自检不过不许进下一步。

项目进度、挂账、关键决策写在**项目侧交接文档**（每个项目自己维护），不放在本包——本包只装方法论。
