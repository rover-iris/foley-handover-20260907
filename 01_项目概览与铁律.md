# 01 · 项目概览与铁律

## 项目是什么

一部动画剧集的音效制作。工作模式：**AI 全自动驱动 REAPER**——读视频动作分析报告 → 从音源库检索选材 → 自动往 REAPER 工程里贴 cue → 导出分轨交付。

项目进度、挂账、关键决策记录放**项目侧交接文档**（每个项目自己维护），不写进本包。

## 关键路径（均为历史项目实例写法；同事机器上以人类开工时提供的实际路径为准）

| 东西 | 路径模式 |
|------|------|
| REAPER 主工程 | `Desktop\<项目名>-音效工程\<项目名>_音效工程.RPP` |
| 逐秒动作分析报告+抽帧 | `Desktop\<项目名>N-N集分析报告\`（每集一个 md + 对应 frames 目录） |
| 分轨导出成品 | `Desktop\<项目名>-分轨导出_48k24b\` |
| 工作脚本目录（历史） | 原机临时工作区（已废弃）——现行脚本一律落本包 `scripts/` 与项目侧 `scripts\` |
| 音源库 DB | 双版本（2026-09-20 起）：本机版 `scripts/pipeline/asset_library.db`（61,397 条，path 前缀 `D:\音效文件\`）；NAS 版 `scripts/pipeline/asset_library_nas.db`（60,871 条，path 即 NAS 路径） |
| 音源实体文件 | 本地 `D:\音效文件\`（多个包，见 config.json 的 `library.roots`）；或 NAS `\\192.168.9.251\音效2\资源库\`（全量音源，其他机器通用） |

> ⚠️ 本机版 DB 里存的 path 指向原机的 `D:\音效文件\...`——音源挂 NAS 的机器改用 NAS 版 DB（path 即 NAS 路径，零改造；526 条 NAS 真缺失已剔除，见工程侧映射报告）。音源路径既不同、又不是这块 NAS 的，才需要重建索引（见 04 号文档）或批量替换路径前缀。

## 工程结构

- 整部片子是**一条时间线**，用 **Region 分集**：每集一个 Region，Region 起点即该集 0 秒。
- 贴轨坐标全部是 `Region 内相对时间`，贴的时候换算成绝对时间：`绝对时间 = Region起点 + t_local`。
- 轨道分组：动作组（15 条子轨：脚步、脚步(D)、衣物摩擦、动作fx 03~14）+ 其他组。分轨导出只导动作组。

## Region 基准（每集开始必做）

- 新集/接手工程的 base 一律**实测**：`scripts/pipeline/extract_regions.py` 离线解析 RPP 生成全剧集基准表（region_bases.csv），或 reapy 枚举 Region 取起点——**不要估算**。
- 视频时长一律 ffprobe 实测；基准表与时长表属项目侧数据，放在项目目录，不进本包。

## 🔴 铁律（违反任何一条都是事故）

1. **NAS `\\192.168.9.251\...` 音效资源绝对只读**——任何情况下不允许写入/修改/删除音效文件；唯一例外 `资源库\_索引\`（索引产物区，2026-09-20 制作人批准：只放资产表 DB 与检索引擎副本）。
2. **音源实体文件只读**——只索引、只取路径，不移动不重命名不改文件。
3. **外部报告不可直接信任**——必须先抽帧对照核验动作是否真实存在（假动作防御），核验通过才贴轨。
4. **贴轨前必须确认工程已备份**——.RPP 是纯文本，改坏前先复制一份。
5. **每批贴轨必须自检**：区间对账（数量核对）+ 重叠检测 + off+len 越界审计，三关全过才算完。
6. **渲染期间不许碰 REAPER**——不许手动暂停、不许切工程，否则渲染内容会被污染（详见 08）。
7. **改任何工程状态前先备份**（推子/mute/solo 状态存 JSON，见 06 号文档）。
8. **音量一律不动**——cue 条目 D_VOL 恒为 1.0，禁止任何调量（2026-09-11 制作人口径；20260916 旧口径复发致 531 条返工，已根治）。
9. **脚步声不归 AI**——脚步/怪物足步/爬行步归【手动】人工，AI 不选材不贴轨（20260916 制作人条款：步频无法与画面对齐）。
10. **start_offset 默认 0**——非零取窗必须先出 ffmpeg volumedetect 证据（窗口 max_volume > −30dB）；>10s 杂集类素材默认不作候选（20260916 条款）。
11. **口径冲突即停即问；数值禁止手抄**——派单与 skill/文档冲突时暂停请示制作人；BASE/偏移只准引用落盘基准表文件（20260916 条款）。
