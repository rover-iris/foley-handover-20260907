# 04 · 检索引擎与选材

## 音源库 DB（双版本，2026-09-20 起）

- **本机版** `scripts/pipeline/asset_library.db`（SQLite + FTS5，44MB，**61,397 条**，path 前缀 `D:\音效文件\`；旧文「61,347」为陈旧计数）。
- **NAS 通用版** `scripts/pipeline/asset_library_nas.db`（**60,871 条**，path 即 NAS `\\192.168.9.251\音效2\资源库\...` 绝对路径，挂 NAS 的机器零改造直接用）。同 sha1 副本两处：NAS `资源库\_索引\asset_library_nas.db`、见习音效师工程（Claude Code 版）`assets\`。由工程侧 `scripts\make_nas_asset_db.py` + 规则表 `nas_path_map.json` 从本机版映射生成（61,397 → 命中 60,871 即 99.14%，526 条 NAS 真缺失剔除进存疑清单，映射报告落工程 `cues\资产表NAS版映射报告_20260920.md`）；schema 与本机版完全一致。
- 检索引擎 `search_assets.py` 原件在本包 pipeline（更新以它为同步源）；副本两处：NAS `资源库\_索引\`（NAS 机器 import 用）与 Claude Code 工程 `scripts\`。用法统一 import 姿势（CLI 直跑有 index/ 硬编码旧坑）。

- 表 `assets`：`name`（文件名）、`path`（绝对路径）、`category`、`rating`（1~5星）等字段。
- FTS5 全文索引支持按关键词搜名字。
- 索引来源：`config.json` 的 `library.roots`（通用包）+ `theme_roots.中国风`（设计包）+ `action_packs`（动作关键词→包的映射）。

## 基本用法（place 脚本里的 q() 模式）

```python
import sqlite3
db = sqlite3.connect(r"...asset_library.db")

def q(name):
    # 精确名优先，前缀匹配兜底
    r = db.execute("SELECT path FROM assets WHERE name=? LIMIT 1", (name,)).fetchone()
    if r: return r[0]
    r = db.execute("SELECT path FROM assets WHERE name LIKE ? LIMIT 1", (name + '%',)).fetchone()
    if not r: raise RuntimeError(f"库里没有: {name}")
    return r[0]
```

关键词检索（交互式选材时用）：

```python
db.execute("""
  SELECT name, path, rating FROM assets
  WHERE name MATCH ? AND category NOT IN (低优先级列表)
  ORDER BY rating DESC LIMIT 20
""", ('"法术" AND "开启"',))
```

## 新机首次引导（2026-09-20 增补）

新机器（或换音源环境）第一次开工时，先做一次性音源库配置再贴轨：

1. 探测音源模式：NAS `\\192.168.9.251\音效2\资源库` 可达 → NAS 模式；`D:\音效文件` 在 → 本地模式；双库并存问人类二选一；都无则停，向人类要路径，禁止猜。
2. 选资产表 db 来源（NAS `_索引\` / 本包 pipeline / 工程侧 `assets\` / 自定义），确认文件真实存在，缺文件给复制指引不静默换。
3. 结果写成**机器级配置**（Claude Code 工程为 `~/.claude/foley_machine_config.json`，`/开始` 命令引导生成；其他 agent 平台记到自己的项目配置）。机器配置只作默认值来源——每集施工的权威仍是 skill 的 per-project `foley_project.json`。
4. bash 里用 python 访问 NAS（UNC 路径）必须写脚本文件执行，禁止 `python -c` 内联（实测静默失败，见 08 号）。

## 路径问题（同事机器必读）

本机版 DB 里的 `path` 是**原机的绝对路径**（如 `D:/音效文件/Boom Library/...`）。三种处理方式：

0. **音源挂 NAS（2026-09-20 起首选）**：直接改用 NAS 版 `asset_library_nas.db`，path 即 NAS 路径，零改造。
1. **音源盘原样迁移**：path 直接能用（推荐，音源包结构不变即可）。
2. **路径不同**：批量替换 DB 里的路径前缀：
   ```sql
   UPDATE assets SET path = replace(path, 'D:/音效文件/', 'E:/音效/') WHERE path LIKE 'D:/音效文件/%';
   ```
3. **全新音源库**：改好 `config.json` 的 roots 后重建索引（见下）。

## 重建索引（仅音源库变化时）

```bash
cd scripts/pipeline
python build_asset_db.py        # 扫描 roots，写入 index/asset_library.db
python search_assets.py "关键词"  # 检索自测（此 CLI 有 index/ 硬编码坑，报错时改用 import 姿势）
```

`embeddings` 功能（向量检索）在 config 里默认关（`build_embeddings: false`），纯关键词+评分检索已够用，不必开。

## 选材终审六问（每条 cue 定稿前必过；2026-09-23 起试行，验证至下次更换新视频）

> 操作层只有这六问。**任何一问拿不准 → 进存疑清单，不硬贴。**六问不再增加条目——新的翻车教训只进末节反例库，防稀释。（与 skill `reaper-foley-workflow/references/retrieval.md` 互为镜像，改动须同步）

1. **事件对位**——配的是帧里真实发生的事件吗？画面判读有疑 → 上报，任何人不改判。
2. **声源存在**——这个声源在画面世界里存在吗？（可见金属件才配金属声；古风画面无机车引擎）
3. **档位对位**——力度×体型档位匹配吗？（cue 轻/弱 ↔ 素材名 Massive/Hard 冲突即存疑；Large/Huge cue 配 Soft/Small 同判）
4. **质感域对位**——题材/元素/材质在同一个世界吗？（题材开工前判定并写入项目 `genre`，检索管线按域标签自动降权对域素材：玄幻不选科技域、金光不配火/电系、金属只配可见金属件）
5. **空间对位**——far/mid/close 与景别远近一致吗？（远山炸开 = far 档，近景摩擦 = close）
6. **结构对位**——movement/impact/whoosh 优先级选对了吗？起手/收尾齐全吗？

### 硬约束（流程层，长期有效，不占六问）

| 约束 | 内容 | 为什么 |
|------|------|--------|
| 集内同源 ≤2 次 | 同一源文件一集最多 2 条 cue；邻近 cue 异窗轮换，同源多次取不同时间窗 | 观众会听出复用 |
| 星级不硬卡（20260916 修订） | 5★ 内部随机仅适用中档以上 impact；弱/轻类 cue 不做星级与 Designed 加权——星级是质量背书，不是匹配度背书 | 避免优质大 hit 顶掉贴切的小声 |
| 锁包检索 | 动作→官方包映射（config `action_packs`）：衣物/打斗→close combat bundle；脚步→virtual foley artist；门→boom doors；水→cinematic fire&water；爆炸→matter mayhem | 几万→几百，又快又准 |
| 动作主词检索（20260916） | 检索以动作词为主词、第一权重；材质词低权重辅助（定类靠动作，收窄靠材质） | 「金属」当主词曾检索出钢铁落地声去配手扶立柱 |
| whoosh/movement 优先级（20260916） | whoosh 第一优先服务转场与明显快速位移；慢速肢体与物品动作 movement 第一优先，whoosh 仅作有快速位移证据时的补充层 | 递信封没有风切声 |
| 脚步不配（20260916） | 脚步/足步/爬行步归【手动】人工，AI 不选材不贴轨 | 步频无法与画面对齐 |
| 杂集末位（20260916） | >10s 集锦类默认不作候选；确需使用须全窗 volumedetect 有声证据 + 存疑放行 | 40s 杂集实测 75% 静音段 |
| 非零 offset 验窗（20260916） | 默认 0 头部起播；取窗须 volumedetect 证据（窗口 max>−30dB） | 盲选窗口 3/4 为数字静音 |

### 反例库（教学层，按问归挂；新教训只加这里）

- **问1 事件对位**：剑身青光实为人物拔剑、浮空碎石实为远山炸开（20260923 测试2 上游改判案）——事件判读错了，音色怎么选都是错。
- **问2 声源存在**：锁子甲 foley 配无金属部件的人物甲叶（20260923 测试2）——身上没有金属件，甲叶声再像也是无源之声。
- **问3 档位对位**：金属撞击配扶立柱轻吱（20260916 事故）；巨魔动作配布料 whoosh（20260922 测试2 开场）；Low Smooth impact 配巨物蓄力——瞬态柔和、质地单薄的素材撑不起巨物量级。机器化落地（力度词冲突自动判存疑）见 skill retrieval.md 问3 展开节。
- **问4 质感域**：force field loop 配玄幻结界（科技域）；火系爆炸配金光爆发（元素错且带电系高频嘶嘶）；「妖气」暗系剑鸣配青光充盈（语义偏差）。
- **问5 空间对位**：近景碎石轻响配远处山脉震动炸开（应 explosion far 档）。

低优先级 category（枪战/猛禽/烟火等，气质不符）在 config.json `low_priority_categories`，检索时排除。**题材域过滤（2026-09-23 试行）**：config `library.domain_tags` 给包/分类打域标签（科技域：SoundMorph Robotic Lifeforms 2、sci-fi bundle、18.【未来科幻高科技】、43.【霓虹 氛围】、69.【未来科幻武器】、77.【未来科技UI】），`genre_domain_block` 定义题材→降权域映射；`search(..., genre="玄幻")` 命中即 -8 压末位（降权非排除，宁可末位不空手）。题材写入 `foley_project.json` 的 `genre` 字段（`project_config.py init --genre`）。手工 SQL 检索自行加 `NOT IN` 等效过滤。

低优先级 category（枪战/猛禽/烟火等，气质不符）在 config.json `low_priority_categories`，检索时排除。

## 资产表结构与分类规则（建库侧，2026-09-09 补录）

assets 表实际 **12 个字段**，每个文件六层提取（详见 `build_asset_db.py`）：

| 字段 | 来源 |
|------|------|
| path / filename / name | 文件系统（name=去扩展名 stem，检索主键） |
| category | 相对根目录的第一段**中文分类目录名** |
| desc | WAV **bext chunk 的 Description**（官方英文描述；自写 RIFF 解析取前 256 字节，不依赖第三方库） |
| duration / samplerate | wave 模块直读 |
| n_cues | 文件内官方 cue 分区数量 |
| theme | 路径前缀匹配 config `theme_roots`（如「中国风」设计包），未命中=`通用` |
| rating | **WAV 尾部 64KB 的 Steinberg BWFXML `MediaRating`**——Cubase/Nuendo 人工评的 1-5 星，正则提取；无评分=0 |
| official | 官方包名：Boom 取中文分类后的官方子目录名（平铺分类则中文分类兜底）；中国风 / Sound Morph 取库名（剥前缀括号） |

**cues 表（一对多，必读）**：一个源文件常按官方 cue 排多个声音（如 40s 文件排 5 个 HIT）。`cues(asset_id, offset_sec, len_sec, label)` 记录每段起点/长度/官方标签——**这是「同源异段取窗」的数据根基**，place 引擎里 take 的 `D_STARTOFFS` 就查这张表。

FTS5：`assets_fts(name, desc, category)` 三列全文索引，触发器同步——搜名字和搜官方描述都能命中。

重建注意事项（`build_asset_db.py`）：
- WAL 模式 + **每文件 commit**（中断不丢进度）；**增量模式**：已入库路径自动跳过，全量重建先删 db 文件；
- `official` 列晚于建库脚本加入，脚本自带存量回填逻辑；
- 全库 6.1 万条约几十分钟扫完。

**库外新资产入库**（用户指定了 DB 索引范围之外的新音效时）：DB 不是全盘扫描，只收 config `roots` 范围内的包。库外文件手写 INSERT 即可，不必全量重建：

```sql
INSERT INTO assets(path, filename, category, name, desc, duration, samplerate, n_cues, theme, rating, official)
VALUES('D:/音效文件/新包/xxx.wav', 'xxx.wav', '新包', 'xxx', '', <时长>, 48000, 0, '通用', 0, '新包');
```

`q()` 按 name 精确+前缀匹配，所以 `name` 取文件名 stem 即可被 place 脚本引用。28 集实战新增的蒸汽嘶/咳嗽/咀嚼等即此法入库。

## 集内经验

- 每集开始先建本集的 `S` 音源字典（像 place_ep26_v1.py 那样，别名→DB 真名），一条 cue 一行可读性最好。
- label 用中文描述动作（"符纸自燃爆燃"）——**仅用于日志/对账打印**；REAPER 条目名一律写源文件名 stem（用户按包定位音源，2026-09-09 拍板）。
- 音量一律默认 **D_VOL=1.0**，不做任何调整（2026-09-11 制作人口径）。历史上「0.5~0.8 / 0.3~0.5」经验值因残存于本文档曾被下游照抄扩散（20260916 夜批 531 条返工）——凡遇旧值即视为废止。

---

## 环境底衬选材与资产库更新（2026-09-11 增补，正文口径）

- **常用环境 category 口径**：默认只用 61.【城镇乡村环境氛围】、16.【大自然环境】、22.【自然 风】、84.【自然氛围】、39.【城市郊区日常】+ 新增 **106.【中世纪环境 古风场景】**；33.【地下环境】、17.【山谷 回声】为低频备用，仅明确对应场景才用。
- **新增资产**：`D:\音效文件\中世纪环境 - Medieval Ambiences Pack [WAV HD]` 已全量入库（50 条，96kHz，category=106，theme=中国风），场景覆盖庄园/城堡/村庄集市/战斗/港口/监狱/草原日夜/雨/风暴/瀑布/沼泽/船舶等 24 组。
- **中文检索关键经验**：FTS5 默认分词不切中文连续串，检索中文素材必须用 desc 里的空格分隔关键词（106 包已逐条标注：庄园/庭院/城堡/白天…）；入库新包时必须逐条写关键词 desc，否则中文查询假阴性。
- **场景定场映射表**（上游报告场景第一秒的「时间+地点」→ 底衬选材）已写入正式 skill `reaper-foley-workflow/references/retrieval.md`「环境底衬选材」节，快照已同步。
- **音量口径联动**：环境底衬同样默认音量不调整，选材时挑本身电平安静的素材（Quietudes 系），检索文档「音量口径」节已同步修订。底衬听感偏大/偏小**不构成撤换或缺配理由**（量级归人工调）；只有底衬缺失、场景不符、质感不对才由 AI 换材补段——环境底衬的选材、换材、补段、贴轨本身是 AI 施工范围。
