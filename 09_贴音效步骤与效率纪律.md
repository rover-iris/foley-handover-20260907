# 09 · 贴音效步骤与效率纪律（给 AI 的工作方式说明书）

> **这篇讲什么**：贴音效（往 REAPER 工程里批量放 cue）每集到底怎么跑，以及——为什么有的 agent 跑这一步极慢、看起来一直在写各种 python 脚本。
> **读完你会明白**：慢的从来不是机器执行，是重复决策。本项目的贴轨管线是「工程定稿一次，每集只是一张数据表」。

---

## 0. 两种工作模式（先看清区别再动手）

| | ✅ 本项目的模式 | ❌ 常见的慢模式 |
|---|---|---|
| 脚本来源 | 定稿模板，**每集只改头部数据区** | 每集从零写新脚本 |
| 音源查找 | SQLite 索引库毫秒级查表 | 现场用 glob/LLM 在文件系统里摸路径 |
| REAPER 操作 | reapy_boost 直连 API | 模拟按键 / 反复试 API |
| API 坑 | 已沉淀进模板和 skill 快照 | 每个坑现场烧几轮「试跑→报错→改→再跑」 |
| 挂了怎么办 | 幂等防重，直接重跑 | 不敢重跑，推倒重写 |
| 每集 AI 真正做的 | 核验报告、选材、填一张数据表、看自检结果 | 重新发明全部逻辑 |

> **一句话总纲：工程定稿一次，每集只是一张数据表。**
> 贴 22 条 cue 的机器执行只要十几秒；一集从报告到贴完也就几分钟。凡是「一直在写脚本」的，时间都烧在了重复决策上。

---

## 1. 什么是一次性工程（做完就冻结，不许每集重写）

以下东西**已经定稿**，存在于本包 `scripts/` 目录，任何会话直接复用：

| 资产 | 位置 | 作用 |
|------|------|------|
| reapy_boost 连接方式 | `skill快照/reaper-remote-ops_SKILL.md` §1 | `connect(Host(IPv4Address("127.0.0.1")))` + sleep(1)，只有这一种可靠写法 |
| 音源索引库 | `scripts/pipeline/asset_library.db`（本机版 61,397 条）/ `asset_library_nas.db`（NAS 版 60,871 条，2026-09-20 起） | SQLite+FTS5，`q()` 函数按名取真实路径；用哪版见 04 号「新机首次引导」 |
| 贴轨模板脚本 | `scripts/示例与工具/place_ep26_v1.py` | 连接/防重/贴轨/D_STARTOFFS/对账 全部定稿 |
| 审计脚本 | `scripts/示例与工具/audit_offsets.py` | 全工程 off+len 越界审计，可续跑 |
| 自检三关 | `05_贴轨脚本与自检.md` | 区间对账 / 重叠检测 / 越界审计 |
| API 坑速查表 | `skill快照/reaper-remote-ops_SKILL.md` §7 | 12 条实测坑，动手前先读 |

**红线：不许为了「这一集的特殊情况」重写模板逻辑。** 特殊情况用数据解决（CUES 表本身就很灵活），解决不了再改模板——改模板属于「发现新坑」，改完必须把新坑写回 skill 快照，一次改动全线受益。

---

## 2. 每集实际执行的五步

### 第 1 步 · 报告帧证据核验
- 输入：逐秒动作分析报告（+ 抽帧图）。
- 做法：对报告里每条 cue 抽对应帧**亲眼确认动作存在**。AI 报告会幻觉假动作，贴上去就是废 cue。
- 产出：核验结论写进脚本头部注释（如「报告已帧证据核验」）。
- 双报告冲突时（新旧报告对同一段动作判读不同），必须抽帧仲裁，不许二选一猜。

### 第 2 步 · 查库选材
- 对 DB 跑 SQL 检索（毫秒级），不是让模型现场想文件名。
- 规则：集内同源 ≤2 次、异窗轮换、玄幻类用正货包；星级不硬卡（弱/轻类不做星级与 Designed 加权）；动作主词检索、力度对位三问；脚步不配（详见 `04_检索引擎与选材.md`）。
- 产出：本集 S 字典（别名 → DB 真名，如 `'FIRE': q("CECK FIRE Whoosh Fireball")`）。

### 第 3 步 · 填 CUES 表
这是每集**唯一真正新写的内容**——一张九元组数据表：

```python
# (track_idx, t_local, source_key, len, label, fadeout, start_offset, vol, fadein)
CUES = [
    (20, 13.2, 'FIRE', 2.0, "符纸自燃爆燃", 0.7, 0, 1.0, None),
    ...
]
BASE = 1606.99   # 本集 Region 起点（用 EnumProjectMarkers2 实测取，不要估算）
```

- 字段含义详见 `05_贴轨脚本与自检.md`。
- label 用中文写清动作（"符纸自燃爆燃"），仅用于日志/对账打印；**REAPER 条目名一律 = 源文件名 stem**（用户按包定位音源，2026-09-09 拍板），防重判定 = 同位置 + 同源文件名。

### 第 4 步 · 跑定稿模板
- 把 BASE 和 CUES 替换进模板，跑。机器执行十几秒。
- reapy 偶发段错误（exit 1 零输出）→ **直接重跑**，防重机制保证不贴重。这是标准恢复手段，不是事故。

### 第 5 步 · 三重自检
1. **区间对账**：Region 内实际 item 数 == CUES 条数（⚠️ 防重跳过会让脚本计数器显示 0，判断成败只认对账）。
2. **重叠检测**：同轨相邻 item 时间窗不重叠。
3. **越界审计**：每条 `off + len ≤ 源时长`，跑 `audit_offsets.py`。
- 三关全过才算完工。任何一关不过 → 修数据表，重跑。

---

## 2.5 · 报告标注类型的处理口径（硬规定，勿按旧版 skill 理解）

| 标注 | 处理 | 依据 |
|------|------|------|
| 【自动】 | 自动配（物理/环境声） | 03 号 SOP |
| 【魔法】 | **自动配，走玄幻正货包**（声羽拾光《法术》《玄幻法器》等，DB category 可查）——不是跳过！ | 04 号「正货优先」；ep3 v2 起限制放开（2026-08-28）；ep26~30 的阵法/法器/灵光 cue 均为自动配实证 |
| 【对白】 | 跳过不配（行保留） | 全部版本一致 |
| 【待定】 | 跳过留空（Q 版卡通段，等卡通音效课定方向） | ep34-36 类段落实证 |
| 【手动】 | UI/人物卡/地点卡/转场字幕类——**人工项**，AI 不擅自配；除非用户明确说用剧集通用 HUD 音 | ep1/8 报告「待用户拍板」惯例 |

> ⚠️ 2026-08-28 之前的旧版 skill 里【魔法】曾是限制项——**以本表（新口径）为准**。若报告把法术类标成【手动】/【待定】（外部报告偏保守），按【魔法】口径放开处理，无需再问。
> ⚠️ 20260916 起：报告【自动】中的**脚步类**（含怪物足步/爬行步）归【手动】人工处理，AI 不选材不贴轨（制作人条款：步频无法与画面对齐）。

---

## 3. AI 的时间该花在哪

| 该花时间的地方 | 不该花时间的地方 |
|---|---|
| 读报告、抽帧核验 | 重写连接代码 |
| 检索选材、权衡音源气质 | 重新查 D_STARTOFFS 怎么写 |
| 把 CUES 表的时间点校准到帧 | 现场摸音效文件路径 |
| 看自检结果、修数据 | 跟 REAPER 窗口焦点搏斗 |

判断标准很简单：**这件事第二集还要不要再做一遍？** 要 → 它属于工程，定稿冻结。不要 → 它属于数据，每集现填。AI 产出的每一行新代码都应该问自己这个问题。

---

## 4. 慢模式的典型症状（自查用）

1. 每集新建 `.py` 文件，里面 import、connect、q() 函数、防重逻辑一应俱全——**这就是在重写模板**。
2. 脚本报错后大改代码结构——通常是被某个已知坑咬了（先查 `reaper-remote-ops_SKILL.md` §7 速查表）。
3. 在对话里反复打印脚本全文——数据表 10 行就够了，逻辑不该出现在对话里。
4. 用 LLM 猜音效文件名/路径——库里 6 万条，查表 1 毫秒。
5. 贴完不敢验证，靠「应该没问题」收工——三重自检是硬性流程。

> 模板和数据都递到手边了。抄模板、改表、跑、验——就这四件事。

---

## 5. 多 agent 派活与批量纪律（20260914/0916 教训沉淀）

- **执行层单层直派**：主对话持自包含口径直接派执行 agent。**嵌套孵化链（总监再孵化见习生）实测慢且不透明，废弃**。
- **子 agent 并发 ≤2 路，默认 1 路串行**：多路齐发实测触发 API 限流（429/1302），ZCode 崩溃整夜中断。宁可慢，不可崩。
- **每集完成即保存工程 + 更新进度文档**：崩溃/中断后按进度从最后完成集续跑，已完成工作禁止重做——夜批零损失全靠这条。
- **派活单硬条款**（写死在派活单里）：音源必查资产表并引用记录（name/rating/duration）；脚本必抄定稿模板只改数据区；禁止猜文件名/glob 摸盘；交班不贴脚本全文。
- **口径冲突即停即问**：执行 agent 上报口径冲突（派单与 skill/文档相抵）时，值班必须暂停请示人类，「按单继续」不是值班可裁决事项（20260916 音量事故：见习上报冲突被值班错误裁决继续，531 条返工）。
- **数值禁止手抄**：BASE/偏移/区间只准引用落盘基准表文件，从对话抄数字曾错 2.375s、69 条返工。

---

## 附 · 第一集重做专属提示（2026-09-09 拍板）

当前任务是用本纪律重做第 1 集（报告：`REAPER工程\视频动作分析\1_视频动作分析.md`）。三条专属事实，开工前读：

1. **BASE = 0**。第一集是 Region 1，就是工程时间线原点，绝对时间 = t_local 本身。不要去遍历搜「Region 起点」——搜出来的第一条就是 0。
2. **法术类已拍板**：该报告「待用户拍板」第 3 条（23-24 施法起手、28-30 法阵、3-6 法术尾迹、64-66 七彩血按正货还是等指定）——**按正货口径直接放开**，走 DB 玄幻正货包自动配，无需再等确认。§2.5 的口径即最终口径。
3. 其余待拍板项维持现状：人物卡/地点卡 HUD 音（第 1 条）不擅自配，等用户明确说用剧集通用版；卡通贴片段（34-36、40-42）按【待定】留空；坠机 56-58 建议多层叠加（树冠爆裂+碎屑+尘土分轨），执行时按多层做但每层独立可审。
