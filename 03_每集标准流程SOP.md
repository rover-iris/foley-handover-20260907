# 03 · 每集标准流程 SOP

> 一集从「拿到报告」到「贴轨完成」的完整流程。每一步的细节在 04/05 号文档。

```
上游报告 → ①帧证据核验 → ②读取Region基准 → ③检索选材 → ④写place脚本 → ⑤执行贴轨 → ⑥三重自检 → ⑦存档
```

## ① 帧证据核验（不可跳过）

- 输入：逐秒动作分析报告（格式规范与出厂门禁见 `skill快照/video-action-analysis/SKILL.md`），报告目录下有对应抽帧 jpg。收报告先跑 `check_report.py` 门禁，FAIL 打回。
- 做法：对报告里每条 cue，抽对应时间点的帧**亲眼确认动作真实存在**。AI 生成报告会幻觉出不存在的动作（假动作），贴上去就是废 cue。
- 核验结论写进 place 脚本头部注释（如 `报告已帧证据核验`），作为质检痕迹。
- 报告里的时间戳若与抽帧对不上（帧率/截取偏移问题），以抽帧实际内容为准修正。

## ② 读取 Region 基准

```python
# 列举所有 region，找到目标集的起点
for i in range(RPR.CountProjectMarkers(0)):
    ok, isrg, pos, _, name, _ = RPR.EnumProjectMarkers2(0, i)
    if isrg and f"{ep:02d}" in name:   # 按实际命名规则匹配
        base = pos
```
**绝对时间 = base + t_local**。所有后续计算都基于这个换算，不要手工加。
- BASE 优先从项目侧落盘基准表读取（如 `region_bases.csv`），禁止从对话手抄数值。
- 工程验收基本项：视频条目数 == Region 数 == 集数（视频入轨时未建 Region 的用 `create_video_regions.py` 补建）。

## ③ 检索选材

用音源库 DB 检索（详见 04 号文档），规则：
- **动作主词检索**：以报告事件主词（动作）为检索主词、第一权重；材质词只做过滤/降权辅助——材质当主词 = 「金属撞击配人动作」事故源头（20260916）。
- **集内同源 ≤ 2 次**：同一个源文件在一集内最多用两次，超了换近义音源。
- **异窗轮换**：相邻窗口（时间上靠近的几条 cue）不要用同一个源。
- **星级不硬卡**：5★ 内部随机仅适用中档以上 impact；弱/轻类 cue 不做星级与 Designed 库加权（星级是质量背书，不是匹配度背书）。
- **力度/质感对位三问**：力度词同档（轻/弱 vs Massive/Hard 同现判存疑）、素材层对位（轻互动声 foley 库优先于电影冲击库）、声源存在性；冲突条目进存疑音色清单，放行后才贴。
- **脚步声不配**：脚步/怪物足步/爬行步归【手动】人工，AI 不选材不贴轨（20260916 条款）。
- **杂集类默认不作候选**：>10s 集锦类素材默认排末位，确需使用须全窗 volumedetect 有声证据 + 放行。
- 玄幻/法术类优先用正货包（声羽拾光等中文设计包，DB 中 category 可查）。

## ④ 写 place 脚本

抄 `scripts/示例与工具/place_ep_template.py` 当模板（当前引擎定稿，强制默认音量；历史示例 place_ep26_v1.py 等仅供对照）。核心是一张 cue 表：

```python
CUES = [
    # (track_idx, t_local, source_key, len, label, fadeout, start_offset, vol, fadein)
    # ⚠2026-09-14 口径：vol 一律写 1.0（默认音量，不做任何调整，引擎强制）
    (20, 13.2, 'FIRE', 2.0, "符纸自燃爆燃", 0.7, 0, 1.0, None),
    ...
]
```

字段逐一说明见 05 号文档。**S 音源字典用 `q()` 从 DB 按名取真实路径**，不要手写绝对路径。
- **start_offset 默认 0**（源头部起播第一优先）；非零取窗必须先出 ffmpeg volumedetect 证据（窗口 max_volume > −30dB），盲选无证据不取窗。

## ⑤ 执行贴轨

- 脚本自带防重（已存在的同位置同 label 的 item 跳过），**可安全重跑**。
- reapy 偶发段错误：直接重跑，防重机制保证不会贴重。
- ⚠️ 防重跳过会让计数器显示 0，**不能以计数器判断成败**，靠 ⑥ 的对账。

## ⑥ 三重自检（全过才算完工）

1. **区间对账**：统计目标 Region 内新增 item 数，与 CUES 长度比对。
2. **重叠检测**：同轨相邻 item 时间窗不得重叠。
3. **越界审计**：每条 cue 的 `off + len` 不得超过源文件时长。跑 `scripts/示例与工具/audit_offsets.py`（全工程审计，可续跑）。

## ⑦ 存档

- place 脚本按 `{工作区}\check\place_ep{NN}_v1.py` 命名留档。
- 工程保存+静默关闭的方法见 08 号文档「保存与关闭」条目。
- 会话收尾把进度写进交接文档（对应人类项目里的 `交接文档_*.md`）。
- 交班报告附**存疑音色清单**：靠关键词匹配但音色无法自证的条目逐条列出，供人工抽听（AI 无试听能力，音色匹配责任上移人工）。

## 批量做的节奏

多集一起做时：每集独立一个 place 脚本，逐集跑完逐集自检，**不要攒到一起跑**——段错误重跑时不好定位是哪集的问题。
