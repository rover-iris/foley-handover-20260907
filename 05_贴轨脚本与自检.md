# 05 · 贴轨脚本与自检

> 模板：`scripts/示例与工具/place_ep_template.py`（**当前引擎定稿**，与 skill 快照一致：强制默认音量/D_STARTOFFS/幂等防重）。历史示例（place_ep26_v1.py 等）仅供对照，抄前看 `scripts/示例与工具/README_使用说明.md`。

## cue 数据格式（定稿，别改结构）

```python
(track_idx, t_local, source_key, len, label, fadeout, start_offset, vol, fadein)
```

| # | 字段 | 含义 | 说明 |
|---|------|------|------|
| 0 | track_idx | 目标轨的索引 | 工程轨序，从 0 数。贴前**必须实测确认轨序没变** |
| 1 | t_local | Region 内相对秒 | 贴时换算 `绝对 = BASE + t_local` |
| 2 | source_key | S 字典的别名 | S 字典负责 DB 查真实路径 |
| 3 | len | 取窗长度（秒） | ⚠️ 不得超过源长，且 off+len ≤ 源长 |
| 4 | label | 中文动作描述 | **仅用于日志/对账打印，不写入条目名**。条目名一律 = 源文件名 stem（用户按包定位音源，2026-09-09 拍板）；防重判定 = 同位置 + 同源文件名 |
| 5 | fadeout | 出淡（秒） | 0 = 硬切 |
| 6 | start_offset | 源文件内起始偏移（秒） | **必须显式写 D_STARTOFFS，见下**；**默认 0**——非零取窗须 volumedetect 证据（窗口 max>−30dB），盲选不取窗（20260916 条款） |
| 7 | vol | 音量（固定 1.0） | **恒写 1.0**——默认音量口径（2026-09-14 制作人拍板，引擎强制，不做任何调整）。⚠️ 抄旧模板（place_ep26_v1 等）时 vol 列真值会生效，必须全改 1.0 或删除赋值行（20260916 事故：531 条被调量） |
| 8 | fadein | 入淡（秒） | None = 不入淡 |

## 🔴 引擎级坑：D_STARTOFFS 必须显式写

`AddMediaItemToTrack` + `AddTakeToMediaItem` + `SetMediaItemTake_Source` 走完，item 是从源文件 0 秒开始放的。**`start_offset` 参数如果只传给取窗逻辑而不写 take 属性，等于没生效**——历史上 ep18~25 八连贴因此 116 条全部取窗失效（off=0）。

正确写法（贴完每条必须执行）：

```python
off = cue[6]  # start_offset
if off:
    RPR.SetMediaItemTakeInfo_Value(take, 'D_STARTOFFS', off)
```

新代码**必须**带这一句。（历史事故：某项目八连贴 116 条因此取窗全部失效——这就是不写 take 属性的下场。）

## 贴轨核心代码骨架

```python
cur = BASE + t_local
item = RPR.AddMediaItemToTrack(trk)
take = RPR.AddTakeToMediaItem(item)
src = RPR.PCM_Source_CreateFromFile(path)
RPR.SetMediaItemTake_Source(take, src)
RPR.SetMediaItemInfo_Value(item, 'D_POSITION', cur)
RPR.SetMediaItemInfo_Value(item, 'D_LENGTH', len)
RPR.SetMediaItemTakeInfo_Value(take, 'D_STARTOFFS', off)      # ← 上面说的坑
RPR.SetMediaItemInfo_Value(item, 'D_VOL', 1.0)  # 2026-09-14：一律默认音量
RPR.SetMediaItemInfo_Value(item, 'D_FADEOUTLEN', fadeout)
if fadein: RPR.SetMediaItemInfo_Value(item, 'D_FADEINLEN', fadein)
RPR.GetSetMediaItemTakeInfo_String(take, 'P_NAME', label, True)  # 写 label
```

## 🔴 引擎级坑：空 item 交替丢失（2026-08-28 实战）

批量贴轨时 take 的 source 赋值可能**交替性丢失**——item 有位置有长度但没有源（空块），播放全静音。`tk.source = source` 这种对象包装赋值不可靠。

**修复三招**：
1. 贴完每条立即读 `takes[0].source.filename` 验证；
2. 失败的用底层 API 重贴：`AddMediaItemToTrack + AddTakeToMediaItem + SetMediaItemTake_Source`（直接传 source id，不走对象包装）；
3. 失败呈完美交替模式时，**同脚本重跑第二轮即全过**（每轮进程状态重置，防重机制保证只补缺的）。

⚠️ **防重盲区**：跳过判定不能只看「同位置有 item」——要连 source 一起验。空块必须删掉重贴（曾发生：第一轮贴上但 source 丢，第二轮被防重挡住，永久漏修）。终验：全工程扫 source filename 存在性 + 重叠比对。

## 防重（幂等，必须保留）

贴之前扫目标区间已有的 item，`label 相同 且 |位置差| < 0.05s` 视为已存在，跳过。
好处：段错误后直接重跑，不会贴重。坏处：计数器会归零失真——**所以判断成败必须靠区间对账，不是靠脚本打印的"新增 N 条"**。

## 两轮重试

对账数量不足时自动再跑一轮（防重保证第二轮只补缺的）。两轮后仍不足则报错退出，人工介入。

## 三重自检（详见 03 号文档 ⑥）

1. 区间对账：Region 内 item 数 == CUES 条数
2. 重叠检测：同轨 item 时间窗无重叠
3. 越界审计：`off + len ≤ 源时长`（`audit_offsets.py`，对 DB 里的源长做全量比对）

## 选材质量关（20260916 新增）

- **力度/质感对位三问**与**存疑音色清单**：详见 04 号文档。place 脚本生成后自检 label 与素材名力度词冲突，自动列清单，总监/制作人放行后才贴。
- **数值引用**：BASE/偏移必须注明来源基准表文件；**口径冲突即停即问**，不得按单硬贴。

## 历史脚本说明（`scripts/示例与工具/`）

| 文件 | 用途 |
|------|------|
| `place_ep_template.py` | **当前引擎定稿**（强制 D_VOL=1.0 / D_STARTOFFS / 幂等防重 / 自检）。新工程一律抄它 |
| `place_ep26_v1.py` / ep27_29_30 / ep28 / 权臣CP01 共 4 份 | 历史示例（vol 赋值行已于 20260916 中和为强制 1.0；自检轨区间是老工程轨序，照抄前必须换新轨序） |
| `place_ep27_29_30_v1.py` | 三集合一跑法（base 各自独立） |
| `place_ep28_v1.py` | 单集，10 条 cue |
| `audit_offsets.py` | 全工程 off+len 越界审计（可续跑） |
| `export_stems_v4.py` | 分轨导出定稿版（见 06 号文档） |
| `render_format_test.py` | 渲染格式探针：2 秒试条 + ffprobe 验证 |
| `create_video_regions.py` | 逐集 Region 幂等创建（视频 API 直贴后的补救；验收查 Region 数==集数） |
| `fader_backup_20260831.json` | 50 轨推子状态备份样本 |
